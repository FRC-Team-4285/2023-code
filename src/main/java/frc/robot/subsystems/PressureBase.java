package frc.robot.subsystems;

public class PressureBase {
    /*
     * tank pressure getter, compressor state getter
     * make this subsystem disallow commands that use air if tank pressure <60psi
     * Battery Leveling PID (???)
     */
    
}
